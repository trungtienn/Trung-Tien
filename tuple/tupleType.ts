//tuple: dataType/size/order
let skills: [string, number] = ['Test', 15];

let skills2: [boolean, string, number];
skills2 = [true, 'Test', 15];
// if we need an optional element, add ? symbol after type and it only work with final element
